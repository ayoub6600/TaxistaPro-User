import 'package:flutter/material.dart';

class CancelBookingsList extends StatelessWidget {
  const CancelBookingsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
      Text("Cancel Bookings List"),
    ]);
  }
}
