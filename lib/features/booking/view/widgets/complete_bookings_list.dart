import 'package:flutter/material.dart';

class CompleteBookingsList extends StatelessWidget {
  const CompleteBookingsList({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
      Text("Complete Bookings List"),
    ]);
  }
}
